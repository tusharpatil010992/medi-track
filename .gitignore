node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Secrets — never commit. Catch every variant, allow only the template.
# .env.local holds the Supabase service-role key, which bypasses RLS entirely.
.env
.env.*
!.env.example

# Local credential notes and scratch files.
# plan/secrets.txt holds the database password and direct connection string;
# mypromtpt.txt holds a generated account password.
plan/secrets.txt
mypromtpt.txt
*secret*.txt
*credentials*.txt

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
